#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir> <env-dir>

BUILD_DIR=${1:-}
CACHE_DIR=${2:-}
ENV_DIR=${3:-}

SYMBOLIC_LINKS=$(git ls-files | xargs -IXXX find XXX -type l)
echo $SYMBOLIC_LINKS

exit 0
